# Jetson-side minimal dependencies
# Sensors & Actuators
fastapi>=0.100.0
uvicorn>=0.20.0
websockets>=12.0
opencv-python>=4.8.0
numpy>=1.24.0
pydantic>=2.0.0
pyserial>=3.5

# YOLO (safety detection)
ultralytics>=8.0.0

# Hardware (installed separately on Jetson)
# jetracer - pip install -e ~/jetracer
# jetcam - pip install -e ~/jetcam
# Jetson.GPIO - system package
